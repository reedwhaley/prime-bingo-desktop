{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main Prime Bingo window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-default-urls"
  ]
}
